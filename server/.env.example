PORT=5000
CLIENT_URL=http://localhost:5173
# Leave MONGODB_URI empty to run locally without MongoDB installed.
# Contact messages are saved to server/data/messages.json in that mode.
# MONGODB_URI=mongodb://127.0.0.1:27017/yohannes-portfolio
